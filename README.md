# HTTP Server from Scratch in Go

This project demonstrates a basic HTTP server built **from scratch** using raw TCP sockets in Go.

## Features
- Raw TCP server using `net`
- Manual HTTP request parsing
- Custom routing system
- Concurrent connection handling using goroutines

## Run Locally
```bash
go run main.go
